안녕하세요..
함동민 입니다.
반갑습니다.
현재 모바일쿠폰 개발 일을 하고 있습니다.
프로그램 경험은 모바일쿠폰시스템 구축, 하이브로드 앱개발 등이 있습니다.
이번 수업은 VUE로 이용해서 하이브로드 앱을 개발하였는데, 개발 하면서 VUE 관하여 더 개발을 수정하거나 발전할 점이 있는지 복습차원에서 신청했습니다.
4주간 수업 잘 부탁드립니다.

답지
1. json이라는 포멧팅을 사용할수 이미 생성된 객체. 사용자가 프로그램 구축하면 공용으로 사용하는 객체
2. let test = new Array(); for(let record in test){console.log(reccord)};
3. let 은 지역변수 와 전역변수가 구분되지만 var 지역변수와 전역변수가 구분 되지 않습니다.
4. let test; if(test){let test; console.log(test);//지역변수의 값을 가져옴}
5. var xhr = new XMLHttpRequest(); xhr.open("GET", url); xhr.onreadystatechange = function(){xhr.responseText;}
6. let json= {'name':'이름','price':1000,'count':1}; for(let record in json){let table = '<table><tr><td>'+record.name+'</td><td>'+record.price+'</td><td>'+record.count+'</td></tr></table>'}
7. document.getElementsByTagName()
8.
9. $("h1").on("click",function(){})
10. 