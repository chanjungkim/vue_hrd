안녕하세요. 
양현수 차장님 잘 계시죠..
이희면입니다.
반값습니다.
현재 삼성 PLM 에서 지금도 프리랜서로 일을 하고 
프로그램은 경험은 여러가지
자바스크립트와 프런트단은 아시겠지만 지금도 C쪽 개발을 많이 하기에 많치는 않습니다.
이번 수업은 이번에 PLM에서 VUE.js를 사용하게 되어서 듣고 싶어서 왔습니다.
요청내용은 뭐 특별한것은 없습니다.

스크립트를 개발을 많이 안해서 잘 모르겠네요.. ^^
-- 답지
1. 
json 객체 var jsonObject = {A:1, B:2, C:3};

함수형 사용자 정의 객체     	
function Person(name, age) {
	this.myName = name;
	this.myAge = age;
}
function main() {
	var person = new Person("Handy", 20);
}

2.<div id=”elem” about=”Elephant”></div>

for (var prop in elem) {
    console.log(prop, obj[prop]);
}
3. let은 블럭 내부에서만 작동함.
4.
5. 서버로 특정 URL을 호출해서 결과를 비동기적으로 받는것...아래는 JQUERY 예제임.  
jQuery.ajax({
           type:"GET",
           url:"http://??????",
           dataType:"JSON",
           success : function(data) {}
		   complete : function(data) {}
		   error : function(xhr, status, error) {}
});

6.var json = { '물건명':'상추', '가격':'1000', '갯수':'100'};

for(key in json) {
    alert('key:' + key + ' / ' + 'value:' + json[key]);
}
7. 

